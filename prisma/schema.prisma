datasource db {
  provider = "sqlite"
  url      = "file:./database.db"
}

generator client {
  provider = "prisma-client-js"
}

model Note {
  id        String   @id @default(uuid())
  title     String
  content   String
  updatedAt DateTime @updatedAt
  token     String   @unique
}
